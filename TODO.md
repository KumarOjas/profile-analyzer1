# ESLint Fixes for Components

## Tasks
- [ ] Fix SimpleLeaderboard.js: Remove unused 'auth' import, unused 'setCurrentUser' state, unused 'dates' variable
- [ ] Fix SocialChallenges.js: Remove unused 'remove' import, move 'sampleChallenges' inside useEffect or use useMemo, remove unused 'joinTeam' function
- [ ] Fix StreakCounter.js: Add 'updateBadges' and 'updateStreak' to useEffect dependency array
- [ ] Run ESLint to verify all errors are resolved
- [ ] Test components for basic rendering and functionality
